export enum FORMAT_EVENT {
  TEXT = "@format/text",
  ALIGN = "@format/align",
}

export enum CREATE_EVENT {
  ELEMENT = "@create/element",
  MEDIA = "@create/media",
}

export enum MODAL_EVENT {
  OPEN = "@modal/open",
  CLOSE = "@modal/close",
}
